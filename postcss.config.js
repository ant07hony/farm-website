const { variants } = require("./tailwind.config");

module.exports = {
    purge: [],
    darkMode: false,
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}